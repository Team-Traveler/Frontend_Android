<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent" /> <!-- EditText의 배경을 투명하게 설정 -->
    <stroke
    android:width="1dp"
    android:color="#000000" />
    <!-- 테두리 색상을 원하는 색으로 설정 -->
    <corners
        android:radius="8dp" />
    <!-- 테두리의 둥글기를 조절하려면 값 수정 -->
</shape>
